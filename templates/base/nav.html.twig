<!-- NAVBAR -->
<nav class="navbar navbar-expand-lg navbar-light bg-light" style="font-size: 1.3rem">
    <div class="container-fluid">
        <!-- Brand -->
        <a class="navbar-brand" href="{{ path('home') }}">
        </a>

        <!-- Toggler -->
        <button
            class="navbar-toggler"
            type="button"
            data-bs-toggle="collapse"
            data-bs-target="#navbarSupportedContent"
            aria-controls="navbarSupportedContent"
            aria-expanded="false"
            aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <!-- Collapse -->
        <div class="collapse navbar-collapse" id="navbarSupportedContent">

            <!-- Navigation -->
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <a class="nav-link active" aria-current="page" href="{{ path('home') }}"
                       style="color: #28A5C5"
                    >
                        Menu
                    </a>
                </li>
                <li>
                    <a class="nav-link active" aria-current="page" href="{{ path('a-propos') }}"
                       style="color: #28A5C5"
                    >
                        A propos
                    </a>
                </li>
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle"
                       href="#"
                       id="navbarDropdown"
                       role="button"
                       data-bs-toggle="dropdown"
                       aria-expanded="false"
                       style="color: #28A5C5"
                    >
                        Catégorie
                    </a>
                    <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
                        <li><a class="dropdown-item" href="{{ path('sondes') }}">ChambreFroide : Sondes</a></li>
                        <li><a class="dropdown-item" href="{{ path('a-propos') }}">A Propos</a></li>
                        <li><a class="dropdown-item" href="{{ path('contact') }}">Nous contactez</a></li>
                        {% if is_granted('ROLE_ADMIN') %}
                            <li><a class="dropdown-item" href="{{ path('admin/utilisateurs') }}">Admin</a></li>
                        {% endif %}
                        <li><hr class="dropdown-divider"></li>
                    </ul>
                </li>
            </ul>
            <form class="d-flex" style="margin-right: 15px">
                <a href="{{ path('contact') }}" class="btn btn-outline-success" type="submit">Nous contactez</a>
            </form>
            <form class="d-flex">
                {% if is_granted('ROLE_USER') %}
                <a href="{{ path('app_logout') }}" class="btn btn-outline-success" type="submit">Se deconnecter</a>
                {% else %}
                <a style="margin-right: 15px" href="{{ path('register') }}" class="btn btn-outline-success" type="submit">S'enregistrer</a>
                <a href="{{ path('app_login') }}" class="btn btn-outline-success" type="submit">Se connecter</a>
                {% endif %}
            </form>
        </div>
    </div>
</nav>